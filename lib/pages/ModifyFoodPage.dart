import 'package:flutter/material.dart';

class ModifyFoodPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Modify Food'),
      ),
      body: Center(
        child: Text('Modify Food Page'),
      ),
    );
  }
}
